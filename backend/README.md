# PathwAI Backend

FastAPI backend for the PathwAI (VoyageAI) platform.

## Setup

1. Copy `.env.example` to `.env` and fill in the values.
2. `pip install -r requirements.txt`
3. `uvicorn main:app --reload`
